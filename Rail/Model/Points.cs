using System;
using System.Windows;

namespace Rail.Model
{
    public static class Points
    {
        //public static Point Move(this Point point, double dx, double dy)
        //{
        //    return point + new Vector(dx, dy);
        //}

        //public static Point Move(this Point point, Point move)
        //{
        //    return point + (Vector)move;
        //}

        //public static Point Rotate(this Point point, double angle)
        //{
        //    angle *= (Math.PI / 180.0);
        //    double sin = Math.Sin(angle);
        //    double cos = Math.Cos(angle);

        //    return new Point(
        //        (cos * point.X) - (sin * point.Y),
        //        (sin * point.X) + (cos * point.Y));
        //}

        //public static Point Rotate(this Point point, double angle, Point center)
        //{
        //    angle *= (Math.PI / 180.0);
        //    double sin = Math.Sin(angle);
        //    double cos = Math.Cos(angle);

        //    return new Point(center.X + cos * (point.X - center.X) - sin * (point.Y - center.Y),
        //                     center.Y + sin * (point.X - center.X) + cos * (point.Y - center.Y));
        //}

        //public static Point CircleCenter(this Point point, double angle, double radius)
        //{
        //    angle *= (Math.PI / 180.0);
        //    double sin = Math.Sin(angle);
        //    double cos = Math.Cos(angle);

        //    return new Point(
        //        point.X - cos * radius,
        //        point.Y - sin * radius);
        //}

        //public static Point CircleCenter(double angle, double radius)
        //{
        //    angle *= (Math.PI / 180.0);
        //    double sin = Math.Sin(angle);
        //    double cos = Math.Cos(angle);

        //    return new Point(
        //        -cos * radius,
        //        -sin * radius);
        //}

        //public static Point CircleCenterX(double angle, double radius)
        //{
        //    angle *= (Math.PI / 180.0);
        //    double sin = Math.Sin(angle);
        //    double cos = Math.Cos(angle);

        //    return new Point(
        //        cos * radius,
        //        sin * radius);
        //}

        //public static Point Circle(double angle, double radius)
        //{
        //    angle *= (Math.PI / 180.0);
        //    double sin = Math.Sin(angle);
        //    double cos = Math.Cos(angle);

        //    return new Point(cos * radius, sin * radius);
        //}

        //public static Point Scale(this Point point, double factor)
        //{
        //    return new Point(point.X * factor, point.Y * factor);
        //}

        //public static double Distance(this Point a, Point b)
        //{
        //    return Math.Sqrt(Math.Pow(b.X - a.X, 2) + Math.Pow(b.Y - a.Y, 2));
        //}

        //public static double Distancex(this TrackDockPoint a, Point b)
        //{
        //    return Math.Sqrt(Math.Pow(b.X - a.X, 2) + Math.Pow(b.Y - a.Y, 2));
        //}
        
    }
}
