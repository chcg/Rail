<UserControl x:Class="Rail.View.RailPlanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Rail.View"
             xmlns:c="clr-namespace:Rail.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1300">
    <DockPanel>
        <ScrollViewer DockPanel.Dock="Right" Width="200" Focusable="False" Background="White" HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Visible" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
            <c:RailMaterialBar Command="{Binding Path=AddTrackCommand}" Tracks="{Binding Path=RailMaterialTracks}"/>
        </ScrollViewer>
        <ScrollViewer Focusable="False" Background="DarkGray" HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Visible" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"  >
        <c:RailPlan 
            Background="LightGreen"
            GroundWidth="{Binding GroundWidth}"
            GroundHeight="{Binding GroundHeight}"
            ZoomFactor="{Binding ZoomFactor}"
            Tracks="{Binding Path=RailTracks}"/>
    </ScrollViewer>
    </DockPanel>
</UserControl>
