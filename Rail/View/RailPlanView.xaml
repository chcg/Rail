<mvvm:DialogView x:Class="Rail.View.RailPlanView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mvvm="clr-namespace:Rail.Mvvm"
        xmlns:ctrl="clr-namespace:Rail.Controls"
        xmlns:local="clr-namespace:Rail.View"
        mc:Ignorable="d"
        Title="RailPlanView" Height="450" Width="600">
    <DockPanel>
        <mvvm:DialogButtonsView DockPanel.Dock="Bottom"/>
        <Grid Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ctrl:PlateControl Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="9" Margin="5" Foreground="Black" BorderThickness="1" BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"
                               Width1="{Binding Width1}"
                               Width2="{Binding Width2}"
                               Width3="{Binding Width3}"
                               Height1="{Binding Height1}"
                               Height2="{Binding Height2}"
                               Height3="{Binding Height3}" />

            
            <Label Grid.Row="1" Grid.Column="0" Content="Width 1"/>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Width1, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
            <Label Grid.Row="1" Grid.Column="2" Content="mm"/>

            <Label Grid.Row="1" Grid.Column="3" Content="Width 2"/>
            <TextBox Grid.Row="1" Grid.Column="4" Text="{Binding Width2, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
            <Label Grid.Row="1" Grid.Column="5" Content="mm"/>

            <Label Grid.Row="1" Grid.Column="6" Content="Width 3"/>
            <TextBox Grid.Row="1" Grid.Column="7" Text="{Binding Width3, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
            <Label Grid.Row="1" Grid.Column="8" Content="mm"/>

            <Label Grid.Row="2" Grid.Column="0" Content="Height 1"/>
            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Height1, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
            <Label Grid.Row="2" Grid.Column="2" Content="mm"/>

            <Label Grid.Row="2" Grid.Column="3" Content="Height 2"/>
            <TextBox Grid.Row="2" Grid.Column="4" Text="{Binding Height2, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
            <Label Grid.Row="2" Grid.Column="5" Content="mm"/>

            <Label Grid.Row="2" Grid.Column="6" Content="Height 3"/>
            <TextBox Grid.Row="2" Grid.Column="7" Text="{Binding Height3, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
            <Label Grid.Row="2" Grid.Column="8" Content="mm"/>

        </Grid>
    </DockPanel>
</mvvm:DialogView>
