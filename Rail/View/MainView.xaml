<mvvm:FileRibbonView x:Class="Rail.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:v="clr-namespace:Rail.View"
        xmlns:vm="clr-namespace:Rail.ViewModel"
        xmlns:ctrl="clr-namespace:Rail.Controls"
        xmlns:mvvm="clr-namespace:Rail.Mvvm"
        xmlns:conv="clr-namespace:Rail.Converter"
        mc:Ignorable="d"
        Height="1000" 
        Width="1600">
    <RibbonWindow.DataContext>
        <vm:MainViewModel/>
    </RibbonWindow.DataContext>
    <RibbonWindow.Resources>
        <conv:TaskbarItemProgressStateToVisibilityConverter x:Key="TaskbarItemProgressStateToVisibilityConverter"/>
        <conv:DoubleToPercentageConverter x:Key="DoubleToPercentageConverter"/>
    </RibbonWindow.Resources>
    <DockPanel>
        <mvvm:PersistentRibbon DockPanel.Dock="Top">
            <Ribbon.ApplicationMenu>
                <RibbonApplicationMenu SmallImageSource="/Rail;component/Images/Rail.png" ToolTip="{Binding Title}">
                    <RibbonApplicationMenuItem Header="New" ImageSource="/Rail;component/Images/New.png" QuickAccessToolBarImageSource="/Rail;component/Images/New.png" Command="{Binding NewCommand}"/>
                    <RibbonApplicationMenuItem Header="Open..." ImageSource="/Rail;component/Images/Open.png" QuickAccessToolBarImageSource="/Rail;component/Images/Open.png" Command="{Binding OpenCommand}"/>
                    <RibbonApplicationMenuItem Header="Save" ImageSource="/Rail;component/Images/Save.png" QuickAccessToolBarImageSource="/Rail;component/Images/Save.png" Command="{Binding SaveCommand}"/>
                    <RibbonApplicationMenuItem Header="Save As..." ImageSource="/Rail;component/Images/Save.png" QuickAccessToolBarImageSource="/Rail;component/Images/Save.png" Command="{Binding SaveAsCommand}"/>
                    <RibbonApplicationMenuItem Header="Help..." ImageSource="/Rail;component/Images/Help.png" QuickAccessToolBarImageSource="/Rail;component/Images/Help.png" Command="{Binding HelpCommand}"/>
                    <RibbonApplicationMenuItem Header="About..." ImageSource="/Rail;component/Images/Info.png" QuickAccessToolBarImageSource="/Rail;component/Images/Info.png" Command="{Binding AboutCommand}"/>
                    
                    <RibbonApplicationMenu.FooterPaneContent>
                        <RibbonButton Label="Exit" HorizontalAlignment="Right" Command="{Binding ExitCommand}" />
                    </RibbonApplicationMenu.FooterPaneContent>
                    <RibbonApplicationMenu.AuxiliaryPaneContent>
                        <RibbonGallery CanUserFilter="False" ScrollViewer.VerticalScrollBarVisibility="Auto" >
                            <RibbonGalleryCategory ItemsSource="{Binding RecentFiles}" MaxColumnCount="1" ColumnsStretchToFill="True" IsSharedColumnSizeScope="True" Header="Recent Files">
                                <RibbonGalleryCategory.ItemTemplate>
                                    <DataTemplate>
                                        <RibbonApplicationMenuItem
                                            Command="{Binding Path=DataContext.RecentFileCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type RibbonWindow}}}" 
                                            CommandParameter="{Binding Path}"
                                            ToolTip="{Binding Path}" 
                                            ImageSource="{Binding Image}"
                                            CanAddToQuickAccessToolBarDirectly="False" >
                                            <RibbonApplicationMenuItem.Header>
                                                <TextBlock Text="{Binding Name}" />
                                            </RibbonApplicationMenuItem.Header>
                                        </RibbonApplicationMenuItem>
                                    </DataTemplate>
                                </RibbonGalleryCategory.ItemTemplate>
                            </RibbonGalleryCategory>
                        </RibbonGallery>
                    </RibbonApplicationMenu.AuxiliaryPaneContent>
                </RibbonApplicationMenu>
            </Ribbon.ApplicationMenu>
            <Ribbon.HelpPaneContent>
                <StackPanel Orientation="Horizontal">
                    <RibbonButton Label="About" Command="{Binding AboutCommand}" SmallImageSource="/Rail;component/Images/Info.png" />
                    <RibbonButton Label="Help" Command="{Binding HelpCommand}" SmallImageSource="/Rail;component/Images/Help.png" />
                </StackPanel>
            </Ribbon.HelpPaneContent>
            <RibbonTab Header="Plan">
                <RibbonGroup Header="Tracks">
                    
                </RibbonGroup>
            </RibbonTab>
        </mvvm:PersistentRibbon>
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="16"/>
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </StatusBar.ItemsPanel>
            <StatusBarItem Grid.Column="0" Content="{Binding StatusText}" ToolTip="Status."/>
            <StatusBarItem Grid.Column="1">
                <ProgressBar Value="{Binding ProgressValue}" Visibility="{Binding ProgressState, Converter={StaticResource TaskbarItemProgressStateToVisibilityConverter}}" Minimum="0.0" Maximum="1.0" Width="120" Height="18" ToolTip="Working progress."/>
            </StatusBarItem>
            <StatusBarItem Grid.Column="2" Content="Zoom" Margin="2,0,2,0"/>
            <StatusBarItem Grid.Column="3" Margin="2,0,2,0">
                <ctrl:DoubleSlider FormatString="P0" Minimum="0.1" Maximum="2" Increment="0.05" Value="{Binding ZoomFactor}" SliderWidth="100" />
            </StatusBarItem>
        </StatusBar>
        <v:RailPlanView DataContext="{Binding RailPlan}"/>
    </DockPanel>
</mvvm:FileRibbonView>
